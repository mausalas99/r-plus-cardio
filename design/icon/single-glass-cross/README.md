# Single glass cross

Faithful to the original reference: **sharp 90-degree cross**, **three tapered rounded base lines**, **80% white glass**, accent gradient field, soft drop shadow only.

## Open this

`r_plus_single_glass_cross.svg` (same as `preview.svg`)

## Spec

- Cross: two intersecting rects, **no corner radius**, 112px arm thickness
- Base lines: rounded caps (`rx` = half height), widths 196 / 124 / 68
- Material: `#ffffff` @ **0.8** on cross + lines together
- No double-pane, refraction blur, pillow gradients, or rim strokes

## Icon Composer layers

1. `layer-1-field.svg`
2. `layer-2-glass-shadow.svg` (optional)
3. `layer-2-glass-mark.svg`

Reference: `assets/r_plus_single_glass_cross-eb0def6d-a32e-44ef-9649-90b0d8ed01ad.png`
